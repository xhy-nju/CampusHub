# ADR 目录

ADR 是 Architecture Decision Record，用来记录关键架构决策。

建议至少维护以下几条：

- `ADR-001-layered-monolith.md`：采用前后端分离 + 分层单体后端
- `ADR-002-postgresql-redis.md`：采用 PostgreSQL + Redis
- `ADR-003-jwt-authentication.md`：采用 JWT 认证

每条 ADR 建议包含：

- 状态
- 背景
- 决策
- 理由
- 后果
- AI 辅助记录和人工修订说明

